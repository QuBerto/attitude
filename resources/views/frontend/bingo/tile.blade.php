<div class="flex flex-col justify-center h-full">
    <div>
        @if(!$bingoglobal)
        <h3 class="text-sm text-center bingotile">{{ $tile->title }}</h3>
        @endif
    </div>
    <div>
        <!-- Add footer or additional content here if needed -->
    </div>
</div>
